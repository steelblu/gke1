---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-buyer
  namespace: shop
spec:
  replicas: 2
  selector:
    matchLabels:
      app: react-buyer
  template:
    metadata:
      labels:
        app: react-buyer
    spec:
      containers:
      - name: nginx
        image: gcr.io/PROJECT_ID/react-buyer:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: react-buyer-svc
  namespace: shop
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  type: ClusterIP
  selector:
    app: react-buyer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
